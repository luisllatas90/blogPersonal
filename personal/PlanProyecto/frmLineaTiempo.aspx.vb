Imports DayPilot.Utils

Partial Class PlanProyecto_frmLineaTiempo
    Inherits System.Web.UI.Page

    Protected Sub Page_Load(ByVal sender As Object, ByVal e As System.EventArgs) Handles Me.Load

        'Response.Write(Server.MapPath("~/web.config"))
        'Dim obj As New TimelineNet.Timeline
        'Dim Evento As New [Event]

        'obj.TimelineData = New TimelineData()
        'Evento.Start = Date.Now
        'Evento.End = Date.Now.AddDays(10)
        'Evento.IsDuration = True
        'Evento.Title = "Linea de Prueba"
        'Evento.Description = "Mensaje de Linea de Prueba"
        'Evento.Color = "Red"
        'Evento.Link = "http://www.google.com.pe/"
        'Evento.Icon = ""
        'Evento.TextColor = "Green"
        'obj.TimelineData.Add(Evento)
        'Dim timeline As New DayPilotCalendar

        'Dim fecha As Date
        'fecha = "31/12/2011"
        'Response.Write(Week.FirstDayOfWeek(fecha) & "<br/>")
        'fecha = "01/01/2011"
        'Response.Write(Week.FirstDayOfWeek(fecha))

        'Response.Write("<cc1:Timeline runat='server' ID='obj' />")
    End Sub
End Class